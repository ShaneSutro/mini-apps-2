/* eslint-disable no-undef */
const Keypad = require('./Keypad.jsx');

describe('Testing the scoring algorithms', () => {
  test('Should be set up correctly', () => {
    const keypad = shallow(<Keypad />)
    expect
    expect(2 + 2).toBe(4);
  });
});
